# Results

This directory contains generated model outputs.

Large CSV/JSON result files are intentionally ignored by Git through
`.gitignore` because they can be reproduced from the notebooks.

If you want to version a particular result, remove its ignore rule or use
Git LFS for large files.
